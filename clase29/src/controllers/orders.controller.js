const getOrders = (req, res) => {
  res.json({ message: 'get orders' })
}

const getOrdersById = (req, res) => {
  res.json({ message: 'get users' })
}

const createOrder = (req, res) => {
  res.json({ message: 'get users' })
}

const resolveOrder = (req, res) => {
  res.json({ message: 'get users' })
}

module.exports = {
  getOrders,
  getOrdersById,
  createOrder,
  resolveOrder,
}
